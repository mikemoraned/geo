[project]
name = "overture-explore"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "overturemaps>=0.12.0",
]

[tool.uv.workspace]
members = ["notebook"]
